{
  "name": "nx-find-matching-config-files-repro",
  "private": true,
  "packageManager": "yarn@1.22.22",
  "scripts": {
    "generate": "yarn nx run repro:generate --",
    "clean": "yarn nx run repro:clean",
    "benchmark:show-projects": "yarn nx run repro:benchmark-show-projects",
    "benchmark:show-projects:perf": "yarn nx run repro:capture-show-projects-perf",
    "compare:show-projects:perf": "yarn nx run repro:compare-show-projects-perf",
    "benchmark:matcher": "yarn nx run repro:benchmark-matcher",
    "show:projects": "yarn nx show projects --json"
  },
  "devDependencies": {
    "minimatch": "^9.0.5",
    "nx": "22.7.3"
  }
}
